name: Test Only

# manual run, test only without linter
# on: 
#   workflow_dispatch:
#     inputs:
#       logLevel:
#         description: 'Log level'     
#         required: true
#         default: 'warning'
#       tags:
#         description: 'Test scenario tags'
# temporary use this, manual run button doesn't appear at actions tab
on: [pull_request]

jobs:
  tests:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        python-version: ['3.6']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install flake8 pytest pytest-cov
        pip install "./src/development[test]" "./src/runtime[all]"
        pip install git+https://github.com/mapillary/inplace_abn.git@v1.1.0
    - name: Test with pytest
      run: |
        pytest -v tests
